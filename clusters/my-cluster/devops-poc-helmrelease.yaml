apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: devops-poc
  namespace: default
spec:
  chart:
    spec:
      chart: devops-poc
      version: '0.1.*'
      sourceRef:
        kind: HelmRepository
        name: devops-poc
      interval: 5m
